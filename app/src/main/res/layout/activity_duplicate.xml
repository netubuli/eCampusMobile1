    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
     android:background="@drawable/background" >
     
       <ImageView
           android:id="@+id/imageView"
           android:layout_width="match_parent"
           android:layout_height="wrap_content"
           android:layout_alignParentRight="true"
           android:layout_marginLeft="2dp"
           android:src="@drawable/logo"
           tools:ignore="ObsoleteLayoutParam,ContentDescription" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_centerVertical="true"
        android:text="Records with the admission number used do exist.click the button below to edit the admission number,else confirm how you are registered."
        tools:context=".DuplicateActivity"
        tools:ignore="ObsoleteLayoutParam,HardcodedText" />
    
         <LinearLayout
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content"
	    android:gravity="left"
	    android:orientation="vertical">

   		 <Button
        android:id="@+id/changeAdm_no"
        android:layout_gravity="center_horizontal"
        android:layout_width="180dp"
        android:layout_height="wrap_content"
        android:text="@string/edit_adm_number"
        android:textStyle="bold"
         />
       
         <Button
             android:id="@+id/regstatusButton"
	        android:layout_gravity="center_horizontal"
	        android:layout_width="180dp"
	        android:layout_height="wrap_content"
             android:text="@string/check_registration_status"
             android:textStyle="bold"
         />

         </LinearLayout> 

</LinearLayout>
